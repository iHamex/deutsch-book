# Hidden Master Formatting Rules - for AI agents only

> This file prevents every formatting mistake we fixed in 2026-09-03/04. Read this before you create or edit ANY markdown file in this vault or in deutsch-book. Not for learner.

---

## 0. HARD BAN - NEVER add AI credit lines to commits (read AGENTS.md at repo root)

This is the most important rule and it overrides everything else. It was added after a violation registered an unwanted third party as a contributor to the owner's personal repo.

**NEVER add any of these to any commit message or file:**

- `🤖 Generated with <tool>` (any tool name, including Codebuff)
- `Co-Authored-By: ...` (any tool, any address)
- `Generated with ...` / `Authored by <AI>` / signatures / emoji credits

**Commit rules for AI agents:**

1. Commit messages are plain text only - summary line + optional body. No credits, no signatures, no emoji.
2. NEVER change the git author/committer identity and NEVER register any account other than the owner as author or co-author.
3. NEVER commit, push, deploy, or rewrite history unless the owner explicitly asked.
4. Final check before any commit: `git log -1 --format=%B` must contain NO "Generated", "Co-Authored", "codebuff", or emoji-credit lines. If it does, fix it - do not push.

Violating rule 0 is unacceptable and irreversible for the owner's GitHub profile. STOP before writing any such line.

---

## 1. Never start a file with admonition before header

**Wrong:**
```md
!!! tip "Title"
    text
# Header
```

**Correct:**
```md
# Header
> intro text
!!! tip "Title"
    text
```

**Check:** `head -n1` must be `# `, never `!!!`.

**Fixed in:** 02-speaking-survival, 04-horen-survival, 05-lesen-survival, 02-exam-writing, 02-opposites

---

## 2. Never use inline · with 3+ items on one line

**Wrong:** `**Prost!** (cheers) · **zahlen** (the bill) · **die Karte** (the menu) · **kaputt** ...`

**Correct:** bullet list inside admonition:
```md
!!! tip "Bonus everyday words"
    - **Prost!** (cheers)
    - **zahlen** (the bill)
    - **die Karte** (the menu)
```

**Check:** `grep -c "·" line >=3 and not table`

**Fixed in:** 06-daily-conversations Bonus everyday words, 08-Practice-Lab logs left as is

---

## 3. Never leave **Header:** without blank line before bullet list

**Wrong:**
```md
**Examples:**
- "I need..."
```

**Correct:**
```md
**Examples:**

- "I need..."
```

Must have blank line between `**Header:**` and first `-`.

**Fixed in:** 02-speaking-survival Examples (3 bullets), 14 files total via comprehensive_fix

---

## 4. Never use `> **Title` + `> -` blockquote list

**Wrong:**
```md
> **The daily trio:**
> - Item 1
> - Item 2
```

**Correct:**
```md
!!! example "The daily trio"
    - Item 1
    - Item 2
```

**Fixed in:** 07-grammar-essentials Focused practice sheets (7 items), 06-daily-conversations daily trio (3), 04-da-wo-compounds Key distinction (2), 02-06-negation Quick test, 03-06-future A2 reality, 03-01-conjugations dauern/mögen

---

## 5. Never use grid cards with `---` inline that renders as single line

**Wrong:** `<div class="grid cards" markdown>` with `:material-numeric-1-circle:` icons that are invalid or missing blank lines around `---`

**Correct:** Use simple table or numbered list for 5 changes:
```md
1. **Drop the -e** - ich habe → **ich hab**
2. **mal = magic word** - *Kannst du mir mal helfen?*
```

**Fixed in:** 06-daily-conversations Spoken German Cheat Sheet (5 changes grid -> numbered list), 07-exam-cheat-sheet Hören/Lesen/Sprechen grid -> table

---

## 6. Never leave `**Bold` without closing `**`

**Wrong:** `**Bonus: everyday words that replace the textbook word`

**Correct:** `**Bonus: everyday words that replace the textbook word**` or `!!! tip "Bonus: ..."`

**Check:** `grep -n "^\*\*[^*]*[^*]$"` - odd count

**Fixed in:** 06-daily-conversations 8 lines (Nämlich, Da … hin split, etc.)

---

## 7. Never use `**Example conversation` as plain bold + blockquote

**Wrong:**
```md
**Example conversation:
> "Line 1"
> "Line 2"
```

**Correct:**
```md
!!! example "Example conversation"
    - "Line 1"
    - "Line 2"
```

Each dialogue must be separate bullet with blank line, not `> "` blockquote without separation.

**Indentation trap:** 4-space indented bullets or quotes are ONLY valid inside `!!!`, `???`, or `===` containers. Anywhere else they render as grey code blocks with visible `**` and `>` marks:

```md
**Example flow:**

    - "**Weißt du,** ..."   <- renders as CODE, broken
```

```md
!!! example "Example flow"
    - "**Weißt du,** ..."   <- renders as bullets, correct
```

**Check:** build the site, then scan rendered HTML for `<pre>` blocks whose text starts with `-`, `*`, `>`, or `**` - every hit is this bug.

**Fixed in:** 06-daily-conversations 8 blocks + 2 indented-code traps (Example flow, Example)

---

## 8. Always separate `### Rule` headings with `---`

**Wrong:** `### Rule 1` table immediately followed by `### Rule 2` with no separator - renders as single paragraph

**Correct:**
```md
### Rule 1: ...
| table |
---
### Rule 2: ...
```

**Fixed in:** 06-daily-conversations 10 Rules

---

## 9. Always use `A1/` level prefix for vault book (scalable structure)

**Structure:**
```
Language/
├── 00-START-HERE.md (vault master)
├── A1/
│   ├── 00-START-HERE.md (A1 book master)
│   ├── 01-Foundations/
│   └── ...08-Practice-Lab/
├── A2/ (future)
└── B1/ (future)
```

Never put `01-` to `09-` at vault root. Always inside `A1/`, `A2/`, etc.

**Fixed in:** 2026-09-03 reorg

---

## 10. Never delete a line - only reorder or wrap

Every fix must preserve every line's content. Only add blank lines, admonitions, or reformat inline lists to bullets. Count lines before/after to verify.

---

## 11. Navigation - mkdocs.yml

- Top tree must have level parents (`A1 Level:`, `A2 Level:`, ...) each containing its Parts. Never flat `Part 01`, `Part 02` at root.
- Part names without `Part 01 -` prefix, just `Foundations`, `Grammar Core`, etc.

**Fixed in:** 2026-09-04 mkdocs.yml nav

---

## 12. Never join two summary statements on one line - one row per line

Two `**Label:**` statements on one paragraph line render as a single run-on line. Each statement gets its own bullet so each renders as its own row.

**Wrong:**

```md
**Total core:** Parts 01-05. **Application:** Parts 06-09.
```

**Correct:**

```md
- **Total core:** Parts 01-05.
- **Application:** Parts 06-09.
```

Tables and example sentences are exempt (a table row IS a row; `Ich **habe** ... **gelernt**.` is one sentence). This rule is only for standalone `**Label:** ... **Label:** ...` paragraph lines.

**Check:** `grep -rn "^\*\*[^*]*:\*\*" docs/ --include="*.md"` - every hit with 2+ bold phrases on one line must be split into bullets.

**Fixed in:** docs/index.md (2 lines), levels/A1/index.md, levels/A2/index.md

---

## 13. Use mkdocs-material visual features - it is the documenting system

This book renders with [mkdocs-material](https://squidfunk.github.io/mkdocs-material/). Use its enabled features to help visually instead of plain bold or blockquotes. What is enabled in `mkdocs.yml`: admonitions, details, content tabs, task lists, mark/highlight, keys, emoji. Mermaid is installed but BANNED by owner decision - never use diagrams of any kind (no mermaid, no ASCII diagrams, no flowcharts). Tables and lists carry structure instead.

**Wrong:**

```md
**Warning: never say "zu Hause gekommen".**
```

**Correct:** admonition matching the message type:

```md
!!! warning "Direction vs location"
    **nach Hause** = direction (Ich gehe nach Hause). **zu Hause** = location (Ich bin zu Hause).
```

Guidance:

- Callouts (tip, warning, danger, note, success, example) as `!!! type "Title"` - never as `**Bold**` + `>` quote (see rule 7).
- Alternatives side by side (textbook vs spoken, formal vs du, Perfekt vs Präteritum) as content tabs, not stacked quotes.
- Checklists (exam day, self-check) as task lists `- [ ]`, not plain bullets, when the learner must tick them off.
- Collapsible extras (`???`) for answers and deep dives so pages stay scannable.
- `==mark==` to highlight the one decisive word in a sentence, not whole-sentence bold.
- Respect existing bans: no grid-cards hack (rule 5).

**Check:** `grep -rn '^\*\*[A-Z][^*]*\*\*$' docs/ --include="*.md"` - a lone bold line is usually a callout that should be an admonition.

**Applies from:** 2026-09-04 (A2 build).

---

## 14. Links carry the exact location - never file-only when a section is meant

A cross-reference that means a section must deep-link to its header anchor (`file.md#section-anchor`), never just the file. Old `:25` line numbers are dead (lines drift) - map each to the nearest header and verify the anchor id in the built HTML.

**Wrong:** `[07-grammar-essentials.md](07-grammar-essentials.md)` for Chance/Zeit/Lust + zu.

**Wrong:** `[07-grammar-essentials.md](../02-Grammar-Core/07-grammar-essentials.md) §16` - section number outside the link never jumps.

**Correct:** `[07-grammar-essentials.md §25](07-grammar-essentials.md#25-zu-infinitiv-the-chance-to-drive-chance-zeit-lust-zu-verb)`.

**Check:** every relative link target must resolve, every `#anchor` must exist as an `id` in the built page. `mkdocs build --strict` must pass with zero warnings. No `](...) §N` pattern may remain: `grep -rnE "\]\([^)]+\) §?[0-9]+" docs/ --include="*.md" | grep -v "#"` must print nothing.

---

## 15. Content rules (from master - never delete these)

These are content and teaching rules, not formatting. They live here because this file is the single source of truth for AI agents.

### 15.1 100% true German only

- Use standard German that is actually correct. No invented words, no invented rules, no "sounds right" guesses.
- If you are not sure a construction is correct, check against existing files in this repo or rephrase with something already proven.
- If a rule is not sure, do not teach it as fact. Mark it as receptive or leave it out.

### 15.2 Level by folder

- A1 files stay A1. They may point forward to A2, but they do not teach A2 grammar as if it were A1.
- A2 files build on A1. They do not repeat A1 as if it were new, and they do not jump to B1 grammar unless it serves a real situation.

### 15.3 One teaching point per section

- A section that tries to teach three things at once is harder to remember and harder to fix.
- Never join two `**Label:**` statements on one paragraph line. Split them into bullets (see rule 12).

### 15.4 No puzzles, quizzes, or guess-the-answer formats

- The book teaches directly. No "guess the ending", no "what is the correct form?", no riddles. Direct coaching only.

### 15.5 Exam vs Daily split (explicit wherever it matters)

- **Exam German:** `Sie`, full forms (`ich habe`, not `ich hab`), present tense only in Schreiben, no spoken shortcuts, no `mal`/`doch` fillers in exam tasks.
- **Daily German:** spoken contractions (`ich hab`, `mach ich`), `mal`, `doch`, fillers (`naja`, `halt`, `eben`, `irgendwie`), V1 statements, da-compounds, `wegen + Dativ` in speech.
- If a file touches both exam and daily German, it states the split explicitly at the top and keeps the two dialects visually separated.
- Tables and examples must make clear which dialect they belong to.
- Exam files stay in exam dialect throughout. Daily files stay in spoken style.

### 15.6 Actionable endings

- Every teaching section that teaches something ends with something actionable: a small cheat sheet, a practice line, or a re-test link back to the mistake log.

### 15.7 Packaging and real-life context

- Examples should be full sentences a learner can memorize and reuse, not fragments that only make sense next to a grammar explanation.
- Where it makes the example real and natural, use the learner's real context: Munich, Check24, Erika-Mann-Straße 12, 80636 München, Hamed Okhovvat, Telc/A1/A2 exam context. Do not force it into every file.
- A teaching file should not end on a theory note alone. End with something the learner can do: a cheat sheet, a short practice prompt, a sentence to produce from memory, or a link back to `mistakes.md`.
- When a file teaches something that learners commonly get wrong, point back to the relevant mistake entry in `08-Practice-Lab/01-mistakes.md`.

### 15.8 A2 INDEX honesty

- An A2 `00-INDEX.md` must not list `(planned)` files as if they are readable. If a file does not exist yet, the INDEX says "planned - next" or otherwise makes clear that the file is not ready.
- An A2 Part index should not present a full reading sequence that ends in missing files.
- Each A2 Part index should say what A1 chunk it builds on and what comes next.

---

## Checklist before you finish any edit

- [ ] `head -n1` is `# `, not `!!!`
- [ ] No line has `·` count >=3 outside table
- [ ] No `> **` + `> -` blockquote lists remain
- [ ] No `**Header` without closing `**`
- [ ] No `**Example conversation` without `!!! example`
- [ ] No `grid cards` with `---` inline - use table/list
- [ ] Every `### Rule` has `---` separator before next
- [ ] Every `**Header:**` + bullet has blank line
- [ ] No paragraph line starts with `**Label:**` and contains a second `**...**` - split into bullets (rule 12)
- [ ] Visual callouts use admonitions/tabs/task-lists, not lone bold lines or blockquotes (rule 13)
- [ ] `grep -n "A1/"` shows correct level prefixes

Run `python3 /tmp/deep_scan.py` - must show 0 or only `08-Practice-Lab/04-daily-practice` logs.
